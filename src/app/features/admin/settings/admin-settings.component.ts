// Phase 1m option-3 — this standalone /admin/configuration page was
// retired in favour of the existing /admin/integrations surface (which
// now drives off IntegrationDescriptorCatalog + ISettingsService).
// File neutered so lint:i18n doesn't pick up dangling key references;
// safe to delete from disk in a follow-up cleanup.
export {};
